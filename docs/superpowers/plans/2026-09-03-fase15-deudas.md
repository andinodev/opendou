# Fase 15 — Deudas C1–C5 — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline, como en las fases anteriores). Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pagar C1 (envío propio de reverb), C3 (spline y multiposición en el sistema de voces), C4 (LUFS nativo) y C5 (latencia del altavoz de mundo); C2 pasa a la Fase 16.

**Architecture:** Un acumulador mono nativo por bus del pool que los streams llenan antes del HRTF y un `AudioEffect` de entrada que lo vuelca al bus; proveedores de posición en la instancia; un `AudioEffect` de medición K por bloques; un test de latencia con clicks.

**Tech Stack:** GDScript 4.7.2, godot-cpp, Steam Audio 4.8.1 (sin API nueva), suite `./run_tests.sh`.

**Spec:** `docs/superpowers/specs/2026-09-03-fase15-deudas-design.md`

## Global Constraints

- Commit gate: `./run_tests.sh` con código de salida 0 (no basta `STATUS: PASSED`).
- Archivos .cpp nuevos en `native/CMakeLists.txt`; clases en `native/src/register_types.cpp`.
- Documentación en español con acentos; comentarios de código en español sin acentos.
- Tests: solo afirmaciones sobre audio medido con controles.

---

### Task 1: C1 — Envío propio de reverb

**Files:** crear `native/src/send_bus.{h,cpp}`, `native/src/reverb_send_input.{h,cpp}`; modificar `native/src/spatial_stream.{h,cpp}`, `native/src/register_types.cpp`, `native/CMakeLists.txt`, `runtime/spatial/reverb_bus_pool.gd`, `runtime/spatial/audio_room.gd`, `nodes/opendou_room_3d.gd`, `runtime/physical_voice_channel.gd`, `runtime/audio_event_manager.gd`, `tests/test_convolution_reverb.gd`, `tests/test_demo_scenes.gd`; crear `tests/test_reverb_send.gd`.

**Interfaces:** `OpenDouSendBus.create() -> int`, `release(id)`, `accumulate(id, mono, n, gain)` (C++), `drain(id, out, n)` (C++); `OpenDouReverbSendInput.send_id`; `OpenDouSpatialStream.set_send(id: int, gain: float)`; pool `send_id_for(bus) -> int`, `install_send_input(bus)`, `has_send_input(bus)`; `AudioRoom.send_id`; canal `set_send(id, gain)`.

- [ ] Test en rojo (`test_reverb_send.gd`, §3 del spec: destino > −20 dB, reverb con cola y sin seco, send 0 mudo; control godot −200).
- [ ] Nativo: `send_bus`, `reverb_send_input`, `set_send` en el stream (acumular tras el campo cercano).
- [ ] Pool/sala/manager/canal según §3. `install_convolution` con `dry = 0` cuando hay entrada de envío.
- [ ] Adaptar `test_convolution_reverb` (tono seco en el bus destino) y el taller de `test_demo_scenes`.
- [ ] Compilar, `./run_tests.sh` verde, commit `"Fase 15: envio propio de reverb en steam_audio; la voz vuelve a su target_bus (C1)"`.

### Task 2: C3 — Proveedores de posición

**Files:** `runtime/event_instance.gd` (`position_provider`), `runtime/audio_event_manager.gd` (`_apply_voices`), `nodes/opendou_spline_emitter_3d.gd`, `nodes/opendou_multi_position_emitter_3d.gd`; crear `tests/test_position_provider.gd`.

- [ ] Test en rojo (§4 del spec).
- [ ] Implementar: instancia + manager; nodos con `event_def`, `play_event()`, `stop_event()`, `resolve_emitter_position`, `resolve_flow_velocity` (spline), sin reproducción propia.
- [ ] `./run_tests.sh` verde (anclaje del spline, multiposición, demos); commit `"Fase 15: spline y multiposicion entran al sistema de voces como proveedores de posicion (C3)"`.

### Task 3: C4 — LUFS nativo

**Files:** crear `native/src/loudness_tap.{h,cpp}`; modificar `register_types.cpp`, `CMakeLists.txt`, `runtime/loudness_meter.gd`, `tests/test_loudness_meter.gd`.

- [ ] Test en rojo: tap nativo da −23.0 LUFS; GDScript forzado ±0.2 LU; coste < 5 ms/s.
- [ ] Implementar `OpenDouLoudnessTap` (§5) y el interruptor en el medidor.
- [ ] Compilar, suite verde, commit `"Fase 15: medidor LUFS con filtro K nativo por bloques (C4)"`.

### Task 4: C5 — Latencia del altavoz de mundo

**Files:** crear `tests/test_world_speaker_latency.gd`; quizá `nodes/opendou_event_player_3d.gd` (búfer del generador).

- [ ] Test: latencia < 250 ms y estable ±20 ms (§6). Anotar la medida.
- [ ] Si > 120 ms, bajar el búfer a 0.1 s y volver a medir.
- [ ] Suite verde, commit `"Fase 15: latencia del altavoz de mundo medida y afirmada (C5)"`.

### Task 5: Documentos

- [ ] `funcionalidades.md`, `AGENTS.md` (observación 54), `current.md`, spec §11, observaciones §C.
- [ ] Suite verde, banco a 200 voces, commit `"Fase 15: documentos al dia; observacion 54"`.
